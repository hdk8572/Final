<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="task">
  
	<select id="selectList" resultType="PtaskVo">
		SELECT * 
		FROM TASK
	</select>

	<select id="ProjectSelectOne" resultType="PtaskVo">
		SELECT * 
		FROM TASK
		WHERE PNO=#{PNO}
	</select>	

	<insert id="insertTask" parameterType="PtaskVo">
		INSERT INTO TASK (TNO, PNO, USERID, TMEMBER, TTITLE, TCONTENT, TSTATUS, TSTARTDATE, TENDDATE)
		VALUES (task_sequence.NEXTVAL, #{pno}, #{userid}, #{tmember}, #{ttitle}, #{tcontent}, #{tstatus}, #{tstartDate}, #{tendDate})
	</insert>

	<delete id="deleteTask" parameterType="PtaskVo">
		DELETE FROM TASK WHERE PNO = #(pno)
  	</delete>

</mapper>
