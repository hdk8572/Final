<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="task">
  
	<select id="selectList" resultType="PtaskVo">
		SELECT * 
		FROM TASK
	</select>

	<select id="ProjectSelectList" resultType="PtaskVo">
		SELECT * 
		FROM TASK
		WHERE PNO=#{PNO}
	</select>	

	<insert id="insertTask" parameterType="PtaskVo">
		INSERT INTO PROJECT (TNO, PNO, USERID, TMEMBER, TCONTENT, TSTATUS, TDATE, TSTARTDATE, TENDDATE)
		VALUES (project_sequence.NEXTVAL, project_sequence.NEXTVAL, #{userId}, #{tmember}, #{tcontent}, #{tstatus}, #{tdate}, #{tstartDate}, #{tendDate})
	</insert>

</mapper>
