<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="chat">
	<!-- 리스트 -->
  <select id="selecuserList" parameterType="string" resultType="ChatVo">
		select mname
		from chat_member
		join users using(userid)
	</select>
<!-- 	<select id="selecuserOne" parameterType="string" resultType="ChatVo">
		select mname
		from chat_member
		join users using(userid)
	</select> -->
</mapper>
