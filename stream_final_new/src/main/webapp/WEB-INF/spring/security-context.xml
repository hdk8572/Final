<?xml version="1.0" encoding="UTF-8"?>
<beans:beans
	xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans 
    https://www.springframework.org/schema/beans/spring-beans.xsd
    http://www.springframework.org/schema/security 
    http://www.springframework.org/schema/security/spring-security.xsd">

	<http pattern="/resources/**" security="none"></http>


	<http auto-config="true" use-expressions="true">

		<csrf disabled="true" />
		<intercept-url pattern="/admin/**"			access="hasRole('RA')" />
		<intercept-url pattern="/user/**" access="hasRole('RM')" />
		<intercept-url pattern="/company/**"			access="hasRole('RC')" />
		<intercept-url pattern="/**" access="permitAll" />
		<form-login login-page="/login"
			login-processing-url="/login.do" 
			username-parameter="userid"
			password-parameter="password" 
			default-target-url="/main" 
			always-use-default-target="true" />
		<logout logout-url="/logout" logout-success-url="/main"
			delete-cookies="JSESSIONID" />
	</http>

	<!-- DB에서 정보를 가져와서 비교를 위한 정보 -->
	<jdbc-user-service data-source-ref="dataSource"
		id="userService"
		users-by-username-query="SELECT USERID AS USERNAME , PASSWORD, ENABLED FROM USERS WHERE USERID =?"
		authorities-by-username-query="SELECT USERID AS USERNAME , AUTHORITY FROM USERS WHERE USER_ID =?" />

	<!-- 실질적으로 계정정보 비교 -->
	<authentication-manager>
		<authentication-provider>
			<user-service>
				<user name="admin" password="{noop}admin" authorities="RA" />
				<user name="user" password="{noop}user" authorities="RM" />
			</user-service>
		</authentication-provider>
	</authentication-manager>

	<!-- <security:authentication-provider user-service-ref="userService"> <security:password-encoder 
		ref="BCryptPasswordEncoder" /> </security:authentication-provider> -->


</beans:beans>